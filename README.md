# test
ai workout planner
# NeuroFit Backend

Python FastAPI backend for the NeuroFit app.
Generates personalized workout plans based on user input and stores them in Supabase.

---

## 🚀 Run Locally
```bash
pip install -r requirements.txt
uvicorn main:app --reload --port 8080
